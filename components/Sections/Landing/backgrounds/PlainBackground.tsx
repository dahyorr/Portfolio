"use client"
/**
 * Switch the active landing background by changing the import below.
 *
 * Available variants (all in ./backgrounds/):
 *   BgMesh      — layered radial mesh gradients + grain noise   (current)
 *   BgGradient  — static multi-stop diagonal linear gradient
 *   BgAnimated  — slow colour-shifting animated gradient
 *   BgPattern   — dot-grid pattern with vignette
 *   BgSplit     — asymmetric diagonal blue wedge on dark navy
 */
export { default } from './BgMesh';
